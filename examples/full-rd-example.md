# Full R&D 示例（完整研发兜底）

## 用户输入示例（需求模糊）
> 优化登录流程，让它更稳定更安全一点

## 期望路由
- 意图：改动型（但目标/范围/约束不清晰）
- 范围：不确定
- 风险：可能涉及认证/安全（潜在 EHRB）
- 模式：Full R&D（先评分追问）

## 追问输出示例（评分 < 7 时）
```
❓【HelloAGENTS】- 需求分析

当前需求完整性评分为 4/10 分，缺少明确目标、成功标准和范围边界。

1. 成功标准是什么？并给一个最小可复现表现/触发条件（报错、超时、偶发失败、触发频率等）
2. 明确不在范围的内容有哪些？（列2条非目标，避免误改）
3. 允许改动哪些层/不允许改动哪些层？（API/数据库/依赖/行为/发布方式等）以及风险容忍度（停机/迁移/回滚）

请按序号回答，或输入“以现有需求继续”跳过追问（可能影响方案质量），或输入“取消”终止。
回复契约: 按序号逐条回答（例如 `1) ... 2) ... 3) ...`），或回复 `以现有需求继续` / `取消`

<helloagents_state>
version: 1
mode: auto
phase: analyze
status: awaiting_user_input
awaiting_kind: questions
package:
next_unique_action: "等待用户补充追问答案/以现有需求继续/取消"
</helloagents_state>
```

## 需求补齐后（评分 ≥ 7）应进入
1. 方案设计：产出完整方案包 `why.md/how.md/task.md`
2. 开发实施：按 `task.md` 执行、测试分级、同步知识库、迁移归档
