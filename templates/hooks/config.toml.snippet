# 将以下片段合并进项目的 `.codex/config.toml`（或全局 `~/.codex/config.toml`）
# 需要同时放置 `.codex/hooks.json`（可从 `templates/hooks/hooks.json` 复制）
# 这是 Codex hooks 的可选接线配置；不配置也不影响 helloagents 核心流程

[features]
codex_hooks = true
